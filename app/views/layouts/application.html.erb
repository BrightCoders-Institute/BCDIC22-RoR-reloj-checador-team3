<!DOCTYPE html>
<html>
  <head>
    <title>BCDIC22RorRelojChecadorTeam3</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <link rel="stylesheet"   href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
  </head>

  <body style="background-color: #d4d8d5;">

    <% if current_page?('/users/sign_in') %> 
      <header class="checkin_header">
      <nav><%= link_to image_tag("FooCorp.png", alt: "Foo Corp", class: 'img-fluid'), front_view_index_path %>    
      </nav>
      </header>
    <%else%>

      <header class="checkin_header">
      <nav><%= link_to image_tag("FooCorp.png", alt: "Foo Corp", class: 'img-fluid'), front_view_index_path %>  
      <% if user_signed_in? %>
        <div class="d-md-flex justify-content-between align-items-center">
          <%= link_to "Return", :back %>
          <%= link_to "Admin menu", admin_admin_menu_path %>
          <%= button_to "Sign out", destroy_user_session_path, method: :delete %> 
        </div>
      <% else %>
        <%= button_to "Sign in", new_user_session_path, method: :get %> 
      <% end %>   
      </nav> 
      </header>
    <%end%>
    <%= yield %>
  </body>
</html>
